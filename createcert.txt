openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout key.pem -out cert.pem


# if __name__ == '__main__':
#     # For local development or testing with a self-signed certificate:
#     #   1. Generate cert.pem and key.pem using OpenSSL.
#     #   2. Place them in the same directory as this script or use the absolute paths.
#     #   3. Update ssl_context to point to your cert and key files below.
#     app.run(
#         debug=True,
#         host="0.0.0.0",
#         port=5000,
#         ssl_context=('cert.pem', 'key.pem')  # Provide paths to your certificate and key
#     )